{
  "name": "tauri-xhr",
  "version": "0.1.2",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "author": "ComfyFluffy",
  "description": "A custom XHR implementation that can be used to bypass CORS limit of the webview using tauri's http feature.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ComfyFluffy/tauri-xhr.git"
  },
  "keywords": [
    "tauri",
    "xhr",
    "request",
    "http",
    "https",
    "cors"
  ],
  "license": "MIT",
  "scripts": {
    "build": "rimraf dist && tsc",
    "prepublish": "yarn build",
    "publish": "yarn build && npm publish"
  },
  "files": [
    "dist",
    "LICENSE.md"
  ],
  "devDependencies": {
    "rimraf": "^5.0.0",
    "typescript": "^5.0.4"
  },
  "dependencies": {
    "@tauri-apps/api": "^1.3.0"
  }
}
